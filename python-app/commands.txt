cat Containerfile

podman login registry.ocp4.example.com:8443

podman build -t nodebug .

podman run --rm -d -p 5000:5000 --name nodebug -p 5678:5678 nodebug sh -c 'pip install debugpy -t /tmp && python /tmp/debugpy --listen 0.0.0.0:5678 --wait-for-client -m flask run --host=0.0.0.0'

codium .

curl localhost:5000

